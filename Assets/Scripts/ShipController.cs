using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShipController : MonoBehaviour
{
    

    // Update is called once per frame
    void Update()
    {
        ///if turn: Turn()
        ///if fire: Fire()
        ///react to wind (locomotion)
        ///go
    }
}
